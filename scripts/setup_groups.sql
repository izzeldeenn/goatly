-- Run this script in your Supabase SQL editor to set up the groups system

-- First, read the groups tables file
-- Then execute the SQL commands to create all necessary tables and functions

-- You can run this by copying the contents of database/groups_tables.sql
-- and pasting them into your Supabase SQL editor

-- After running the SQL, your groups system will be ready to use!
